<li>
	{{burger_name}}

	<button class="change-devour" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}Eat Me!!{{else}} Put on the plate!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">EW! Toss in Trash!</button>
</li>
