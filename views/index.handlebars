<h1>Burgers!</h1>

<h2>Burgers that are on the plate!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that were devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devour=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="burger_name">
  </div>

  <div class="form-group">
    <label for="devoured">This burger...?</label><br>
    <input type="radio" name="devoured" value="1" checked> Was Devoured<br>
    <input type="radio" name="devoured" value="0"> Goes back on the plate!
  </div>

  <button type="submit">Add Burger</button>
</form>
